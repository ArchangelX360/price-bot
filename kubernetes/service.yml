apiVersion: v1
kind: Service
metadata:
  name: price-bot
  namespace: price-bot
  labels:
    app: price-bot
spec:
  ports:
    - name: bot
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: price-bot
  type: ClusterIP
